import { Link, Nav, Header } from "./Navigation.styled";

const Navigation = () => {
  return (
    <Header>
      <Nav>
        <Link to="/">UCourse</Link>
      </Nav>
    </Header>
  );
};

export default Navigation;
